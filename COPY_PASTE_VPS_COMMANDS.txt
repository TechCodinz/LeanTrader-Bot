╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                  📋 COPY/PASTE THESE TO YOUR VPS                             ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝


🔍 STEP 1: QUICK DIAGNOSTICS (Run these first!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

cd ~/trading_bot

# What commit are you on?
git log --oneline -1

# Is bot running?
ps aux | grep RUN_BOT.py | grep -v grep

# What systems are loaded?
grep -E "ALL.*SYSTEMS INITIALIZED" COMPLETE_ULTIMATE_ORCHESTRATOR.py | tail -1

# Any recent errors?
tail -50 bot.log | grep -E "ERROR|WARNING" | tail -10


═══════════════════════════════════════════════════════════════════════════════


✅ OPTION A: ROLLBACK TO WORKING VERSION (Oct 20 - SAFE!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This is the version you SHOWED ME that was working!
- 26 core systems ✅
- 7 advanced systems ✅  
- Making trades with 94% confidence ✅
- All engines active ✅

cd ~/trading_bot
git stash  # Save any local changes
git checkout 67a9f5b
pkill -9 -f RUN_BOT.py && sleep 2
./start_bot.sh && sleep 3 && tail -f bot.log

# Press Ctrl+C to stop watching logs
# Bot will keep running in background


═══════════════════════════════════════════════════════════════════════════════


🆕 OPTION B: USE LATEST VERSION (With Dynamic Pair Discovery)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Has all features + dynamic 5000+ pair discovery
But may need debugging if GPT-5 agent broke something

cd ~/trading_bot
git stash  # Save any local changes
git checkout cursor/discover-profitable-trading-pairs-5d1e
git pull origin cursor/discover-profitable-trading-pairs-5d1e
pkill -9 -f RUN_BOT.py && sleep 2
./start_bot.sh && sleep 3 && tail -f bot.log

# Look for: "✅ Dynamic Pair Discovery ready"


═══════════════════════════════════════════════════════════════════════════════


📊 VERIFY BOT IS WORKING (Run after starting)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Watch for trading decisions (should see within 30 seconds)
tail -f bot.log | grep -E "Decision|TRADE|Signal"

# Check all systems initialized
tail -200 bot.log | grep -E "SYSTEMS INITIALIZED"

# Check for errors
tail -200 bot.log | grep ERROR


═══════════════════════════════════════════════════════════════════════════════


🎯 WHAT TO LOOK FOR (GOOD SIGNS):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ "ALL 26 SYSTEMS INITIALIZED" or "ALL 40 SYSTEMS"
✅ "ALL 7 NEW SYSTEMS INITIALIZED" or "ALL 8 ADVANCED SYSTEMS"
✅ "🎯 Decision: BUY/SELL [PAIR]"
✅ "Evolution engine now learning"
✅ "Starting continuous learning loop"
✅ No errors about missing modules


❌ BAD SIGNS (means something broke):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ "ImportError" or "ModuleNotFoundError"
❌ "AttributeError" 
❌ Bot exits immediately
❌ No trading decisions after 2 minutes
❌ Logs stop updating


═══════════════════════════════════════════════════════════════════════════════


🔧 TROUBLESHOOTING COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Check Python environment
source venv/bin/activate
python3 -c "import ccxt; import pandas; import numpy; print('✅ Key packages OK')"

# Test orchestrator import
python3 -c "from COMPLETE_ULTIMATE_ORCHESTRATOR import CompleteUltimateOrchestrator; print('✅ Orchestrator imports')"

# Check git branches
git branch -a | grep -E "main|discover-profitable"

# See what changed in current commit
git show HEAD --stat

# View all recent commits
git log --oneline -10


═══════════════════════════════════════════════════════════════════════════════


💡 MY RECOMMENDATION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. START WITH OPTION A (working version 67a9f5b)
   → Get bot making money FIRST
   → Confirm everything works

2. THEN we can add features one by one
   → Add dynamic pair discovery carefully
   → Test each addition
   → Keep what works, revert what breaks


═══════════════════════════════════════════════════════════════════════════════


📝 AFTER STARTING BOT:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Tell me:
1. Which option you chose (A or B)
2. What the logs show (paste last 30 lines)
3. Any errors you see

Then I'll guide you to:
- Optimize trading
- Add dynamic pair discovery (if using Option A)
- Fix any issues (if using Option B)
- Get bot to maximum profitability!


═══════════════════════════════════════════════════════════════════════════════
